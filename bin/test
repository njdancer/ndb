#!/bin/node
var repl = require('repl');

var Model = require('../lib/Model');

var Post = new Model('/Users/dancer/.ndb/data/Posts');

repl = repl.start();
repl.context.Post = Post;
repl.context.debug = function() {
  post = Post.create();
  post.title = 'test';
  post.save(function() {
    Post.find(post.id, function(data) {
      console.log(data);
      // Post.destroy(post.id, function() {
      //   console.log(post.id);
      //   try {
      //     Post.find(post.id, function(data) {
      //       console.log(data);
      //     });
      //   }
      //   catch(e) {
      //     console.log(e);
      //   }
      // });
    });
  });
};